#include <bits/stdc++.h>
using namespace std;
using ll = long long;

/*
01 -> 1
11 -> 0
00 -> 0
异或
*/


int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int n, i;
    string s, t;
    cin >> n >> s >> t;
    if(s == t) {cout << 1 << '\n'; return 0;}
    if(n == 1)
     
    return 0;
}